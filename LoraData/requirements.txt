# CatTalk2D LoRA Training Requirements
# Install with: pip install -r requirements.txt

# Unsloth (CUDA 12.1 버전 - GPU 필요)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Core ML libraries
torch>=2.1.0
transformers>=4.36.0
datasets>=2.16.0
accelerate>=0.25.0
peft>=0.7.0
bitsandbytes>=0.41.0
trl>=0.7.0

# Optional: For evaluation
evaluate>=0.4.0
scikit-learn>=1.3.0
